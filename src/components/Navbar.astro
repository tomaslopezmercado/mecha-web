<nav class="nav" role="navigation" aria-label="Navegación principal">
  <a href="#" class="nav-logo display" aria-label="mecha · inicio">
    <span>mecha</span>
    <svg
      width="8"
      height="8"
      viewBox="0 0 40 40"
      class="nav-spark"
      aria-hidden="true"
    >
      <line x1="20" y1="2" x2="20" y2="38" stroke="#c4f000" stroke-width="5" />
      <line x1="2" y1="20" x2="38" y2="20" stroke="#c4f000" stroke-width="5" />
      <line x1="6" y1="6" x2="34" y2="34" stroke="#c4f000" stroke-width="5" />
      <line x1="34" y1="6" x2="6" y2="34" stroke="#c4f000" stroke-width="5" />
    </svg>
  </a>

  <div class="nav-links mono" role="list">
    <a href="#servicio" role="listitem">SERVICIO</a>
    <a href="#proceso" role="listitem">PROCESO</a>
    <a href="#preguntas" role="listitem">PREGUNTAS</a>
  </div>

  <div class="nav-right">
    <a href="https://wa.me/" class="nav-cta" rel="noopener noreferrer">
      Hablemos por WhatsApp <span class="arrow" aria-hidden="true">→</span>
    </a>
    <button
      class="nav-hamburger"
      aria-label="Abrir menú"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>
</nav>

<!-- Mobile menu -->
<div id="mobile-menu" class="mobile-menu mono" hidden>
  <a href="#servicio">SERVICIO</a>
  <a href="#proceso">PROCESO</a>
  <a href="#preguntas">PREGUNTAS</a>
  <a href="https://wa.me/" class="mobile-cta" rel="noopener noreferrer">
    Hablemos por WhatsApp →
  </a>
</div>

<script>
  const hamburger = document.querySelector<HTMLButtonElement>('.nav-hamburger');
  const mobileMenu = document.getElementById('mobile-menu');

  hamburger?.addEventListener('click', () => {
    const isOpen = mobileMenu?.hidden === false;
    if (mobileMenu) mobileMenu.hidden = isOpen;
    hamburger.setAttribute('aria-expanded', String(!isOpen));
    hamburger.classList.toggle('is-open', !isOpen);
  });

  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      if (mobileMenu) mobileMenu.hidden = true;
      hamburger?.setAttribute('aria-expanded', 'false');
      hamburger?.classList.remove('is-open');
    });
  });
</script>

<style>
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 32px;
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.06);
    position: sticky;
    top: 0;
    background: #f5f5f2;
    z-index: 50;
  }

  .nav-logo {
    display: inline-flex;
    align-items: baseline;
    text-decoration: none;
    color: #1c1c1c;
    font-size: 20px;
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .nav-spark {
    margin-left: 2px;
    transform: translateY(-11px);
  }

  .nav-links {
    display: flex;
    gap: 28px;
    font-size: 11px;
    color: #4a4a48;
    letter-spacing: 0.08em;
  }

  .nav-links a {
    color: inherit;
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .nav-links a:hover {
    color: #1c1c1c;
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .nav-cta {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    padding: 8px 14px;
    background: #1c1c1c;
    color: white;
    border-radius: 4px;
    font-weight: 500;
    text-decoration: none;
    transition: transform 0.2s ease;
  }

  .nav-cta:hover {
    transform: scale(1.03);
  }

  .nav-cta .arrow {
    color: #c4f000;
    font-size: 14px;
    line-height: 1;
  }

  /* Hamburger */
  .nav-hamburger {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 22px;
    height: 15px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
  }

  .nav-hamburger span {
    display: block;
    width: 100%;
    height: 1.5px;
    background: #1c1c1c;
    transition: transform 0.2s ease, opacity 0.2s ease;
    transform-origin: center;
  }

  .nav-hamburger.is-open span:nth-child(1) {
    transform: translateY(6.75px) rotate(45deg);
  }

  .nav-hamburger.is-open span:nth-child(2) {
    opacity: 0;
  }

  .nav-hamburger.is-open span:nth-child(3) {
    transform: translateY(-6.75px) rotate(-45deg);
  }

  /* Mobile menu */
  .mobile-menu {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: #1c1c1c;
    z-index: 40;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 32px;
    padding: 80px 32px 32px;
  }

  .mobile-menu a {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.75);
    text-decoration: none;
    letter-spacing: 0.1em;
    transition: color 0.15s ease;
  }

  .mobile-menu a:hover {
    color: white;
  }

  .mobile-menu .mobile-cta {
    margin-top: 16px;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    padding: 12px 20px;
    background: #c4f000;
    color: #1c1c1c;
    border-radius: 4px;
    font-weight: 500;
    letter-spacing: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .nav {
      padding: 14px 20px;
    }

    .nav-links {
      display: none;
    }

    .nav-cta {
      display: none;
    }

    .nav-hamburger {
      display: flex;
    }
  }

  @media (max-width: 480px) {
    .nav-hamburger {
      display: flex;
    }
  }
</style>
